import{redirect}from'next/navigation';import{AppShell}from'@/components/app-shell/AppShell';import{ManagerTabbedWorkspace}from'@/components/manager/ManagerTabbedWorkspace';import{buildProtectedAppNavigation}from'@/lib/app-shell/navigation';import{canUse,hasAtLeastView}from'@/lib/auth/access';import{requireDoorGoProtectedAccess}from'@/lib/auth/protected-access';import{createAuthenticatedSupabaseServerClient}from'@/lib/supabase/server';import type{ManagerCapacityConfiguration}from'@/lib/manager/capacity-configuration';import type{ManagerCapacityExceptions}from'@/lib/manager/capacity-exceptions';import{getCurrentDateInTimeZone}from'@/lib/production-board/date-utils';
export default async function ManagerPage({searchParams}:{searchParams?:Promise<Record<string,string|string[]|undefined>>}){const access=await requireDoorGoProtectedAccess();if(!hasAtLeastView(access,'settings'))redirect('/account');const raw=(await searchParams)?.year;const currentYear=Number(getCurrentDateInTimeZone('America/Vancouver').slice(0,4));const requested=Number(Array.isArray(raw)?raw[0]:raw);const year=Number.isInteger(requested)&&requested>=2020&&requested<=2100?requested:currentYear;const client=await createAuthenticatedSupabaseServerClient();const[configResult,exceptionsResult]=await Promise.all([client.rpc('load_manager_capacity_configuration'),client.rpc('load_manager_capacity_exceptions',{p_year:year})]);if(configResult.error)throw new Error(`Failed to load Manager configuration: ${configResult.error.message}`);if(exceptionsResult.error)throw new Error(`Failed to load Manager capacity exceptions: ${exceptionsResult.error.message}`);const configuration=configResult.data as ManagerCapacityConfiguration;return <AppShell navigation={buildProtectedAppNavigation(access)} scrollOwner="workspace"><ManagerTabbedWorkspace canEdit={canUse(access,'settings')} configuration={configuration} exceptions={exceptionsResult.data as ManagerCapacityExceptions}/></AppShell>}

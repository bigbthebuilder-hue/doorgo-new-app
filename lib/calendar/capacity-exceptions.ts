import type{ProductionBoardCard,ProductionBoardViewModel}from'../production-board/types';
export type CalendarCapacityException={recordId:string;kind:'closure'|'holiday'|'special_day';title:string;details:string|null;startDate:string;endDate:string;revision:number;capacityHours:number;occurrences:string[];staffNames:string[]};
export type CalendarCapacityExceptionPayload={records:CalendarCapacityException[];exceptionalDates:string[]};
export function capacityExceptionCard(record:CalendarCapacityException,date:string):ProductionBoardCard{return{bookingId:`capacity:${record.kind}:${record.recordId}:${date}`,recordKind:'capacity_exception',calendarItemType:record.kind,revision:record.revision,type:'biztrack_only',typeLabel:'BizTrack-only',productionDate:date,dayOrder:-2000000,title:record.title,customer:record.title,jobId:null,calendarId:null,calendarEventId:null,shopHours:null,shopHoursKnown:true,salesperson:null,source:'manager_capacity',sourceSystem:'native',bookingKind:record.kind,locked:true,completedAt:null,details:record.details,capacityExceptionId:record.recordId,capacityExceptionStartDate:record.startDate,capacityExceptionEndDate:record.endDate,capacityExceptionCapacity:record.capacityHours,capacityExceptionStaff:record.staffNames};}
export function mergeCapacityExceptions(board:ProductionBoardViewModel,payload:CalendarCapacityExceptionPayload):ProductionBoardViewModel{const byDate=new Map<string,ProductionBoardCard[]>();for(const record of payload.records)for(const date of record.occurrences){const values=byDate.get(date)??[];values.push(capacityExceptionCard(record,date));byDate.set(date,values);}const update=(days:ProductionBoardViewModel['days'])=>days.map((day)=>({...day,cards:[...(byDate.get(day.date)??[]).sort((a,b)=>a.title.localeCompare(b.title)),...day.cards]}));return{...board,days:update(board.days),weekGroups:board.weekGroups.map((week)=>({...week,days:update(week.days)}))};}

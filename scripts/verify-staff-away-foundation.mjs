import assert from'node:assert/strict';import{readFileSync}from'node:fs';
const sql=readFileSync('supabase/migrations/20260825040000_add_staff_away_operations.sql','utf8');const workspace=readFileSync('components/CalendarWorkspace.tsx','utf8');const editor=readFileSync('components/calendar/StaffAwayEditor.tsx','utf8');const queries=readFileSync('lib/production-board/queries.ts','utf8');const actions=readFileSync('lib/calendar/staff-away-actions.ts','utf8');const presentation=readFileSync('lib/calendar/presentation.ts','utf8');
for(const token of ['CREATE TABLE public.dg_staff_away_periods','CREATE TABLE public.dg_staff_away_events','full_day','partial_drag_hours','revision bigint','deleted_at','ENABLE ROW LEVEL SECURITY','permission_key=\'production\'','SECURITY DEFINER SET search_path=\'\'','load_staff_away_calendar_range','save_staff_away_period','delete_staff_away_period','dg_recalculate_staff_away_capacity','native_staff_away_baseline_available_hours','native_staff_away_deduction_hours','dg_daily_capacity_apply_staff_away'])assert.match(sql,new RegExp(token));
assert.doesNotMatch(sql,/permission_key='settings'|is_manager|closure|special_day|holiday/i);assert.doesNotMatch(sql,/GRANT (?:INSERT|UPDATE|DELETE|ALL) ON public\.dg_staff_away/);
assert.match(sql,/effective_from<=p_date[\s\S]*ORDER BY latest\.effective_from DESC LIMIT 1/);assert.match(sql,/dg_staff_away_is_working_day/);assert.match(sql,/p_start_date<>p_end_date/);
assert.match(queries,/loadStaffAwayRange\(params\.boardStart,params\.boardEndExclusive\)/);assert.match(actions,/canUse\(access,'production'\)/);assert.doesNotMatch(actions,/settings|is_manager/);
assert.match(workspace,/StaffAwayEditor/);assert.match(workspace,/recordKind!=='staff_away'/);assert.match(editor,/Capacity Drag Hours/);assert.match(editor,/entire Staff Away period/);assert.match(presentation,/key: 'other:away'.*available: true/);
console.log('Staff Away foundation static verification passed');
